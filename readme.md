# Plants vs Smart Zombies

**By: Kevin Lai**

## Requirements
- Python 3.6 (For `cmu_112_graphics` compatibility)

## How to Run the Game

### Step 1: Install Modules
Run the following three commands in your terminal if you do not have these modules installed:

```bash
pip install requests
pip install pillow
pip install image
